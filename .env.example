# LAN-ready example: publish on every host IPv4 interface. Anyone on the trusted LAN can control the
# miner and its Twitch session, so do not port-forward this service or use it on an untrusted network.
TWITCH_DROPS_BIND=0.0.0.0

# Accept literal private/link-local IP addresses used to open the app from another LAN device.
# Mutation origins must still match the requested server IP and port.
TWITCH_DROPS_ALLOW_LAN=true

# Host-side port. The container always listens on 8080.
TWITCH_DROPS_PORT=8080

# The root JVM listener is loopback-only by default. Compose explicitly overrides this to 0.0.0.0
# inside the container; TWITCH_DROPS_BIND controls host publication.
# TWITCH_DROPS_LISTEN_HOST=127.0.0.1

# Optional: comma-separated Host authorities and browser origins accepted by the app. LAN mode
# automatically accepts literal private IP addresses, but LAN DNS/mDNS names must be listed here.
# Do not add public names unless an authenticated TLS reverse proxy protects them. Forwarded headers
# are never trusted. Keep loopback hosts for the container health check and use exact HTTPS origins
# for proxy deployments.
# TWITCH_DROPS_TRUSTED_HOSTS=127.0.0.1,localhost,[::1],drops.example.com
# TWITCH_DROPS_TRUSTED_ORIGINS=https://drops.example.com

# Optional: override the small-service JVM profile for unusually large inventories.
# TWITCH_DROPS_JAVA_OPTS=-Xms16m -Xmx256m -XX:+UseSerialGC -XX:MaxMetaspaceSize=96m -XX:ReservedCodeCacheSize=32m -XX:MaxDirectMemorySize=32m -Dfile.encoding=UTF-8

# Optional: provide through a secret manager, not a committed .env file.
# Must be a base64-encoded 32-byte key.
# TWITCH_DROPS_SESSION_KEY=
